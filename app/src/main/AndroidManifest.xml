<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.matthewz.test6">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".AActivity" />
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BActivity" />
        <activity android:name=".CActivity" />
        <activity android:name=".RevealActivity" />
        <activity android:name=".RevealActivity2" />
        <activity
            android:name=".RevealActivity3"
            android:theme="@style/noAnimTheme" />
        <activity android:name=".RevealActivity4" />
        <activity android:name=".TransitionActivity" />
        <activity android:name=".TransitionActivity2"></activity>
    </application>

</manifest>